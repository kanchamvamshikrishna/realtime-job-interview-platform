# Leave unset for local dev (Vite proxies /api and /socket.io to localhost:4000).
# Set to the deployed backend's origin (no trailing slash) for production builds,
# e.g. https://job-interview-platform-api.onrender.com
VITE_API_URL=
